#include "main.h"
/**
 * print_alphabet_x10 - print out alphabets 10 times
 *
 * Description: return the required results
 *
 * Return: return void
 */

void print_alphabet_x10(void)
{
	int start;
	char u;

	for (start = 0; starte < 10; start++)
	{
		for (u = 'a'; u <= 'z'; u++)
		{
			_putchar(i);
		}
		_putchar('\n');
	}
}
