#include "main.h"

/**
 * add - adds two integers and returns the result
 * @y: first int to add
 * @z: second int to add
 *
 * Return: returns the result
 *
 */

int add(int y, int z)
{
	return (y + z);
}
